栈有两个操作:弹栈和压栈
遵循后到先出的原则
应用:
1.平衡符号的使用,就如同idea检查自己代码的错误,通过一个空栈来读入文件直到末尾,如果文件是一个开放式文件,将其推入栈中.如果是一个封闭的符号,则当栈空时报错
否则将弹出,如果弹出的开放式符号与之不对应则报错,如果文件结尾栈不为空则报错
2.后缀表达式,通过将符号以后缀的形式表达,元素以压栈弹栈的方式计算,可以直接获得最佳的计算顺序(两种方式:括号和弹压栈)(相等就压下去,大于就弹走小于就放在上面,最后自上到下放入)
3.方法的调用:一样的先调用的方法压在下面
